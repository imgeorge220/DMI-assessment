/**
 *
 * Button
 *
 */

import React from 'react';
import StyledButton from './StyledButton';

function Button() {
  return (
    <StyledButton />
  );
}

Button.propTypes = {};

export default Button;
